{
  "name": "ganymede-atom-syntax",
  "theme": "syntax",
  "version": "2.0.0",
  "description": "An Atom Editor port of Ganymede",
  "keywords": [
    "syntax",
    "theme"
  ],
  "repository": "charlespeters/ganymede-atom-syntax",
  "license": "MIT",
  "scripts": {
    "postpublish": "apm publish ganymede-atom-syntax",
    "prepublish": "push-dir --dir . --branch master --force --remote git@github.com:charlespeters/ganymede-atom-syntax.git"
  },
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
